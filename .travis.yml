language: python
python:
  - "2.7"
  - "3.4"
# command to install dependencies
before_install:
  - cd src
install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script: tox  
